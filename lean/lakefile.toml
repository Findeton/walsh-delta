name = "WalshDelta"
defaultTargets = ["WalshDelta"]

# NOTE (template): pin `mathlib` to a specific rev and set `lean-toolchain`
# to the matching `leanprover/lean4` version before building. Then run:
#   lake exe cache get   &&   lake build
# This project has NOT been built/compiled in the environment where it was
# authored (no Lean toolchain present); see STATUS.md.

[[require]]
name = "mathlib"
scope = "leanprover-community"
git = "https://github.com/leanprover-community/mathlib4.git"
# rev = "<pin-me>"

[[lean_lib]]
name = "WalshDelta"
